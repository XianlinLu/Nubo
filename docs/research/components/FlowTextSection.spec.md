# FlowTextSection

- Height: 508 px.
- Center-aligned SVG path.
- Copy “AI that lets knowledge flow into you” repeats along the path.
- Text color: muted gray; 12–14 px.
- A simple CSS/SVG path is used rather than a bitmap.
