# BeFreed desktop page topology

Reference: https://www.befreed.ai/
Primary viewport: 1440 × 900
Observed document height: 10,388 px

1. `HeaderNav` — fixed, 72 px, transparent navigation bar.
2. `HeroSection` — 828 px. Freedia mascot, 90 px display headline, animated wave, CTA, learner counter, scroll cue.
3. `KnowledgeCloud` — two 900 px scenes. Seventeen media tiles drift around the headings “From the best knowledge sources” and “narrated just for you”.
4. `FlowTextSection` — 508 px. A thin, vertically curling line of repeated copy joins the knowledge cloud to the audio player.
5. `AudioTopicsSection` — 687 px. Large background quote, active audio card, five selectable topic pills, and a CTA.
6. `FeatureShowcase` — 2,468 px. Five alternating two-column feature rows on a galaxy background.
7. `CommunitySection` — 1,921 px. Founder badge, community count, six portrait video cards, CTA, and a 4 × 3 testimonial grid.
8. `DownloadSection` — 690 px. Bordered dark gradient panel containing rating, display copy, download CTA, and a phone screenshot.
9. `Footer` — dark information footer with brand block, four link columns, information links, and an oversized BeFreed wordmark.

The primary scroll narrative is continuous. Header controls and CTAs remain normal links or buttons. The topic selector is the principal stateful interaction on the page.
